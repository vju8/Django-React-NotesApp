// constants used to store and access the access and refresh tokens from frontend localStorage
// ACCESS_TOKEN can be fetched with localStorage.access and REFRESH_TOKEN with localStorage.refresh
export const ACCESS_TOKEN = "access"; 
export const REFRESH_TOKEN = "refresh";